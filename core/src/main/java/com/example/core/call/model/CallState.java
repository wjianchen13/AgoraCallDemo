package com.example.core.call.model;

public enum CallState {
    IDLE,
    LOGGING_IN,
    READY,
    CALLING,
    RINGING,
    ACCEPTING,
    PREPARING,
    JOINING,
    WAITING_REMOTE,
    CONNECTED,
    RECONNECTING,
    LEAVING,
    FAILED
}
